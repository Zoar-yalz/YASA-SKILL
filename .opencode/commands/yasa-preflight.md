---
description: Check whether YASA is installed and configured.
agent: yasa-checker
---

Run YASA environment preflight only.

Do not run a scan.

Check `.yasa-agent.json`, `YASA_ENGINE`, `YASA_WRAPPER`, PATH, local `.yasa-tools/`, `uastSDKPath`, and workspace/report writability.
