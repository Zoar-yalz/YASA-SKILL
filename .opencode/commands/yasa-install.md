---
description: Install the public YASA-Engine release into .yasa-tools after confirmation.
agent: yasa-checker
---

Install YASA-Engine from the public GitHub release only after explicit user approval.

Default command:

```bash
python yasa-checker/scripts/install_yasa_release.py --version v0.3.1 --install-dir .yasa-tools
```

Arguments:

$ARGUMENTS
